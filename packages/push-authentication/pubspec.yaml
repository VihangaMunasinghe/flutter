name: asgardeo_push_auth
description: Push-notification-based authentication SDK for Asgardeo (WSO2 Identity Server).
version: 0.0.1
homepage: https://wso2.com/asgardeo/
topics:
  - authentication
  - push-notification
  - asgardeo
  - wso2
  - mfa

# Workspace member — resolved against the root pubspec.lock.
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  device_info_plus: ^12.4.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  http: ^1.2.2
  local_auth: ^3.0.1
  pointycastle: ^4.0.0
  shared_preferences: ^2.3.5

dev_dependencies:
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
